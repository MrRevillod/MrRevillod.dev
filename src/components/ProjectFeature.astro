---
interface Props {
    cardTitle: string;
    cardText: string;
}

const { cardTitle, cardText } = Astro.props;
---

<div
    class="rounded-md w-full h-full px-8 py-8 flex flex-col items-start justify-center gap-1"
>
    <div>
        <h3>{cardTitle}</h3>
    </div>

    <p class="text-sm">{cardText}</p>
</div>
