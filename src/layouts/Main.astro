---
interface Props {
    customClasses?: string;
}

const { customClasses } = Astro.props;
---

<main
    class=`flex flex-col w-10/12 md:2/3 lg:w-7/12 pt-10 gap-10 md:gap-12 lg:gap-16 ${customClasses}`
>
    <slot />
</main>
